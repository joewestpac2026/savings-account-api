spring:
  datasource:
    url: ${TEST_DB_URL:${DB_URL:jdbc:postgresql://localhost:5432/savings_db}}
    username: postgres
    password: 19901114
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  cache:
    type: simple
