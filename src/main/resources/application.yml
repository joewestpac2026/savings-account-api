spring:
  application:
    name: savings-account-api
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/savings_db}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  cache:
    type: simple

server:
  port: 8080
